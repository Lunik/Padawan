[Unit]
Description=Dynamic DNS and Reverse DNS for IPv6
After=network.target

[Service]
ExecStart=/bin/bash /etc/init.d/padawanv6 start
Type=simple
PIDFile=/run/padawanv6.pid
ExecStop=/bin/bash /etc/init.d/padawanv6 stop

[Install]
WantedBy=multi-user.target
